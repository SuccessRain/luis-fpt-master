# Simple Go library for LUIS.AI

## Install

```
$ go get -u github.com/SuccessRain/luis-fpt-sdk
```

luis test -t intent -i (file_path) -key (my_key) -appid (app_id)
luis train -t intent -i (file_path) -key (my_key) -appid (app_name)